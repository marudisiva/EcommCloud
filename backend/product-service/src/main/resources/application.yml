spring:
  application:
    name: product-service
  data:
    mongodb:
      uri: "mongodb://${MONGO_USER:root}:${MONGO_PASS:root}@${MONGO_HOST:mongodb}:${MONGO_PORT:27017}/${MONGO_DB:product_db}?authSource=admin"

